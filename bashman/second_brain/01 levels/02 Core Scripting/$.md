---
tags: [bash-course, concept, level-02]
level: "[[02 Core Scripting]]"
---

# $?

> $? holds the exit code of the most recently executed command, and must be checked immediately — running any other command overwrites it.

## Syntax
```bash
command
echo $?
```

## Example
```bash
$ grep "error" app.log; echo $?
```

## Expected output
```
0
```

## ⚠️ Common mistake
Running an unrelated command (even echo) between the command you're checking and reading $?, which overwrites it with the wrong value.

## 💡 Practical use
Used to build custom error handling: if [ "$?" -ne 0 ]; then echo "command failed"; fi immediately after a critical step.

## Notes

## Related
- [[02 Core Scripting]]
- [[Bash Cheat Sheet]]
